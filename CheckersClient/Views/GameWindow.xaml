<Window x:Class="CheckersClient.Views.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Шашки Онлайн - Игра" Height="800" Width="900"
        WindowStartupLocation="CenterScreen"
        MinHeight="750" MinWidth="850">

    <Window.Resources>
        <!-- Стиль кнопки шашки -->
        <Style x:Key="CheckerButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                CornerRadius="8"
                                BorderBrush="Gray" 
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E6F3FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#B3D9FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20" Margin="10" CornerRadius="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{Binding CurrentPlayer}" 
                           FontSize="28" FontWeight="Bold" 
                           Foreground="White" VerticalAlignment="Center"
                           Margin="0,0,20,0"/>
                <TextBlock Text="{Binding Status}" 
                           FontSize="18" FontWeight="SemiBold" 
                           Foreground="#3498DB" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Игровое поле -->
        <Border Grid.Row="2" Margin="20" BorderBrush="#34495E" BorderThickness="4" CornerRadius="15">
            <Grid Background="#ECF0F1">
                <UniformGrid Rows="8" Columns="8">
                    <ItemsControl ItemsSource="{Binding Board}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="8" Columns="8"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource CheckerButtonStyle}"
                                        Background="{Binding Background}"
                                        Command="{Binding DataContext.CellClickCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Row}">

                                    <!-- Шашка (круг) -->
                                    <Ellipse Width="58" Height="58" Stroke="Black" StrokeThickness="2">
                                        <Ellipse.Fill>
                                            <SolidColorBrush Color="{Binding PieceWpfColor}"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </UniformGrid>
            </Grid>
        </Border>

        <!-- Нижняя панель -->
        <Grid Grid.Row="3" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StatusBar Grid.Column="0" Height="40" Background="#3498DB" Foreground="White" FontSize="14">
                <StatusBarItem>
                    👆 Кликните на свою шашку → кликните на пустую клетку по диагонали
                </StatusBarItem>
            </StatusBar>

            <Button Grid.Column="1" Content="🏠 Главное меню" 
                    Click="Button_Click" 
                    Padding="20,10" FontSize="16" FontWeight="Bold"
                    Background="#E74C3C" Foreground="White" 
                    Margin="10,0" BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>
