<Window x:Class="CheckersClient.Views.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Шашки онлайн" Height="700" Width="750" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30" HorizontalAlignment="Center">
            <TextBlock Text="{Binding CurrentPlayer}" FontSize="28" FontWeight="Bold" 
                 Foreground="DarkBlue" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Status}" FontSize="20" Margin="30,0" 
                 VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Доска 8x8 -->
        <Border Grid.Row="1" BorderBrush="DarkGoldenrod" BorderThickness="4" CornerRadius="10" 
            Margin="30" Padding="5">
            <ItemsControl ItemsSource="{Binding Board}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Строка из 8 клеток -->
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" Columns="8"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Одна клетка -->
                                    <Button Padding="0" 
                                            Background="{Binding Background}"
                                            Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.CellClicked}"
                                            CommandParameter="{Binding}">
                                        
                                        <Ellipse Width="45" Height="45" Stroke="Black" StrokeThickness="1.5"
                                                 Fill="{Binding PieceWpfColor}"
                                                 Visibility="{Binding PieceVisibility}"/>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Инструкция -->
        <TextBlock Grid.Row="2" 
               Text="👆 Кликните по своей шашке, чтобы сделать ход" 
               FontSize="16" FontWeight="SemiBold"
               HorizontalAlignment="Center" Margin="30" 
               Foreground="DarkGray"/>
    </Grid>
</Window>
